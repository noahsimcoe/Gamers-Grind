const router = require('express').Router();
const HighlightsBoard = require('../../models/HighlightsBoard');

// GET ROUTE - Find all of the highlights
route.get('/', async (req, res) => {
    try {
        const high
    }
})

module.exports = router;
